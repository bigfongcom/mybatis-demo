<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bigfong.mybatis.params.mapper.ClientMapper">
	
	<select id="login1" resultType="Client">
		select * from client where username = #{username} and password = #{password}
	</select>
	<select id="login2" resultType="Client">
		select * from client where username = #{username} and password = #{password}
	</select>
	<select id="login3" resultType="Client">
		select * from client where username = #{username} and password = #{password}
	</select>
</mapper>